apiVersion: networking.istio.io/v1alpha3
kind: DestinationRule
metadata:
  name: {{ include "frontend.fullname" . }}-dr
  namespace: {{ .Release.Namespace }}
spec:
  host: frontend-app-frontend-chart.default.svc.cluster.local
  trafficPolicy:
    tls:
      mode: ISTIO_MUTUAL