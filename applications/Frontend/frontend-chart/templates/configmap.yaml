apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "frontend.fullname" . }}-config
  namespace: {{ .Release.Namespace }}
data:
  index.html: |
{{ .Files.Get "config/index.html" | indent 4 }}
  nginx.conf: |
{{ .Files.Get "config/nginx.conf" | indent 4 }}