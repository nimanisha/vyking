apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "frontend.fullname" . }}-config
  namespace: {{ .Release.Namespace }}
data:
  index.html: {{ tpl (.Files.Get "../index.html") . | indent 4 }}
  nginx.conf: {{ tpl (.Files.Get "../nginx.conf") . | indent 4 }}